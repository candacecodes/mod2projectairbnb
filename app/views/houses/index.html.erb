<h1>House Index</h1>

<h4>Search By: </h4>

Bedrooms
   <%= form_tag(houses_path, method: :get) do %>
      <%= text_field_tag :bedrooms, params[:bedrooms] %>
      <%= submit_tag ("Search") %>
   <% end %>

 Bathrooms
   <%= form_tag(houses_path, method: :get) do %>
      <%= text_field_tag :bathrooms, params[:bathrooms] %>
      <%= submit_tag ("Search") %>
   <% end %>

  Number of Parking Spots
   <%= form_tag(houses_path, method: :get) do %>
      <%= text_field_tag :parking, params[:parking] %>
      <%= submit_tag ("Search") %>
   <% end %>

  Guest Capacity
   <%= form_tag(houses_path, method: :get) do %>
      <%= text_field_tag :guest_limit, params[:guest_limit] %>
      <%= submit_tag ("Search") %>
   <% end %>

Search for Stays that are Pet Friendly
   <%= form_tag(houses_path, method: :get) do %>
      <%= text_field_tag :pet_friendly, params[:pet_friendly] %>
      <%= submit_tag ("Search") %>
   <% end %>

House Type
   <%= form_tag(houses_path, method: :get) do %>
      <%= text_field_tag :house_type, params[:house_type] %>
      <%= submit_tag ("Search") %>
   <% end %>

Cost per Day
   <%= form_tag(houses_path, method: :get) do %>
      <%= text_field_tag :cost_per_day, params[:cost_per_day] %>
      <%= submit_tag ("Search") %>
   <% end %>
<br>
<h4>All Available Houses: </h4>
   <% @house.map do |house| %>
      <ul>
        <li><%= link_to house.address, house_path(house) %></li>
     </ul>
  <% end %>